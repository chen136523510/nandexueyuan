# AGENTS.md — 男德学院项目指令

> 本文件是 ZCode 在本仓库的统一行为指令。`.trae/rules/` 下的规则是 Trae IDE 用的原始来源，本文件为其 ZCode 适配版（精炼 + 路径修正）。

## 项目概览

**男德学院** - 朋友圈限定社区（约 20 人），核心模块「德塔」正在重构中（原 2D 游戏设计已废弃，改为基于原世界观的 视觉小说 方向）。

- 线上地址：https://www.nandexueyuan.top
- 仓库：`chen136523510/nandexueyuan`（GitHub，私有）
- 开发模式：**单人双机协作**（白机白天 / 黑机晚上），详见下文「双机协作」
- 交接单：`.ai/handoff.md`（每次换机必读）
- 路线图：`pm/ROADMAP.md`（战略级，必读）

## 技术栈与启动

| 层 | 技术 | 端口 |
|---|------|------|
| 前端 | Vue 3 + Vite 6 + Pinia | 4396 |
| 游戏前端 | Phaser 4 | - |
| 游戏后端 | Colyseus 0.16 | 2567 |
| API 后端 | Express + Prisma | 3000 |
| 数据库 | SQLite | - |

```bash
# 三个终端分别启动
npm run dev                      # 前端 → localhost:4396
cd server && npm run dev         # API 后端 → localhost:3000
cd game-server && node src/index.js  # 游戏服务器 → localhost:2567
```

**包管理器**：项目 `package.json` 声明 `pnpm`，但实际脚本用 `npm`。新装依赖统一用 `npm install <pkg>`，不要混用 yarn/pnpm 命令以免锁文件冲突。

## Git 工作流（单人仓）

- **主干**：`master`，单人仓**不设主干保护**，可直接 commit + push
- **功能开发**：从 master 切 `feature/<描述>` 分支，完成后合并回 master
- **操作前必做**：静默执行 `git branch --show-current` 和 `git status`，禁止推测上下文
- **合并前必做**：`git fetch origin && git pull origin master`，避免覆盖远端

### Commit 信息规范

格式：`[类型](模块): 动作及意图`

| 标识 | 用途 |
|------|------|
| `[feat]` | 新功能（代码） |
| `[fix]` | 修复 bug（代码） |
| `[refactor]` | 重构（不改外部行为） |
| `[docs]` | 文档变更 |
| `[chore]` | 杂务/构建/依赖 |
| `[文档]` | 需求/设计文档（PRD、ADR） |

- **禁止** `update`/`fix`/`修改` 等无信息量模糊词
- 动词开头，说清改了什么、为什么
- 合并提交（`Merge branch ...`）由 git 自动生成，无需套格式

## 部署纪律（红线）

**未经用户明确指示，禁止执行任何部署到生产环境的操作。**

- 「明确指示」= 用户说出：部署到正式 / 部署到生产 / 上线 / 发布
- 开发完成后只能：本地构建验证（`npm run build`）、本地运行测试、提交代码
- **严禁**自行执行 `scp`、`rsync`、`systemctl restart`、`pm2 restart`、`ssh ... deploy.sh` 等命令
- 用户要求部署时，先确认目标环境，确认后再执行

部署参考：`bash deploy.sh`（构建前端 + 重启 Express 和 game-server PM2 进程）。

## 文档与 Changelog 纪律（强制）

**代码改了但文档没更新 = 任务未完成。**

完成任何代码修改后，必须同步更新：

| 变更类型 | 需更新的文档 |
|---------|-------------|
| 新增功能/需求 | `prd/01-需求文档/04-德塔/changelog.md` + 相关需求文档 |
| 缺陷修复 | `prd/01-需求文档/04-德塔/changelog.md` + `prd/01-需求文档/04-德塔/bug-log.md` |
| 架构变更 | 根 `CHANGELOG.md`（记录决策依据 + 替代方案 + 影响评估） |
| 接口变更 | changelog + 相关技术方案文档 |

### Changelog 层级

- **架构级** → 根 `CHANGELOG.md`（新增/删除目录层、技术栈变更、跨层重构）
- **文件级** → 各层 `changelog.md`（如 `src/api/changelog.md`、`server/src/routes/changelog.md` 等，新增层时同步创建）
- 所有 changelog **倒序排列**（最新在最上方）

### Bug 修复必须登记 bug-log

任何运行时错误登记到 `prd/01-需求文档/04-德塔/bug-log.md`，字段：发现时间、环境、现象、**根因**、修复、教训。

## 双机协作（项目核心特色）

单人开发者，白天白机（荣耀便携本），晚上黑机（RTX 4070 主力机 + ComfyUI），通过 GitHub 中央仓库协同。

**两机能力对等**：均可开发、合并、push master、部署上线。

### 身份判定（每次会话必做，先于一切）

接手任何任务前，先通过当前时间判断自己是白机还是黑机：

| 时间段 | 身份 | 设备 |
|--------|------|------|
| 周一至周五 09:00~19:00 | **白机**（白天） | 荣耀便携本 |
| 其余时间（工作日晚间 + 周末全天） | **黑机**（晚上） | RTX 4070 主力机 + ComfyUI |

判定方式：`date "+%A %H:%M"` 读取系统时间，按上表对号入座。判定后：
1. 在 handoff.md 顶部「所在设备」字段如实填写该身份
2. 后续所有产出记录、commit 署名均以该身份为准
3. 两机能力对等，身份仅决定「当前由谁产出」，不限制操作权限

### 换机铁律

接手第一件事（身份判定之后）：
0. **判定身份**（见上「身份判定」），确认自己是白机还是黑机
1. `git fetch origin && git pull origin master`
2. **先合并对方遗留的 PR**（如有），再开始自己的开发
3. 阅读以下文件恢复上下文（**按顺序**）：
   - `.ai/handoff.md` -- 战术级交接（上次做到哪、进行中任务、环境状态）
   - `pm/ROADMAP.md` -- 战略级路线图（整体往哪走、当前里程碑）

### 山丘图标注规则（AI 行为约束）

ROADMAP.md 中每个任务标注状态，AI 据此决定行为：

| 标注 | 含义 | AI 行为 |
|---|---|---|
| ⛰️ uphill | 探索中，方案未定 | **禁止直接写代码**，先做 PoC/调研/方案验证 |
| ⛰️ downhill | 方案已定，可预估 | 可直接执行，按步骤清单推进 |
| ✅ done | 已完成 | 不再修改，仅可查阅 |

handoff.md 的"待办"部分也应标注 uphill/downhill。

### 会话结束必做

1. `git push` 所有提交到 feature 分支或 master
2. 更新 `.ai/handoff.md`（当前分支、未完成事项、下一步、环境状态）
   - **时间精确到时分**：`最后更新`行与各「本轮产出」节标题一律写 `YYYY-MM-DD HH:MM`（本地时间）。同日双机多轮交接时用于区分先后；无法回填的旧记录保持原样，不得编造
3. 执行 `sync-docs` 技能同步四文档（changelog/bug-log/需求池/handoff）
4. 若里程碑状态变化，更新 `pm/ROADMAP.md`（状态+完成日期+产出物）

## 禁止事项

1. **禁止 TRAE SOLO 徽章注入** — 不得在 `package.json`/`vite.config.js`/`index.html` 中添加 `vite-plugin-trae-solo-badge` 或任何 TRAE 推广内容。发现立即删除。
2. **禁止提交敏感文件** — `.env`、`askass.bat` 等已在 `.gitignore`，不得 `git add` 强制入库。
3. 自行部署、跳过文档更新 — 见上文「部署纪律」和「文档与 Changelog 纪律」。
4. **禁止美工图片入库** — 白机不处理任何美工相关工作，图片不得进入白机。美工图片（ComfyUI/Seedream 出图等）属于黑机本地资产，已纳入 `.gitignore`（`.ai/seedream-test/`、`.ai/comfyui-output/`、`.ai/comfyui-workflows/`、`raw/`）。**严禁** `git add` 这些目录下的任何图片。最终入库的只能是 `public/` 下经抠图处理的正式游戏资产。
5. **禁止未经确认擅自调用 AI 生成 API（红线）** — 任何图片生成（Seedream）或视频生成（Seedance/MiniMax）的 API 调用，**必须先将最终提示词和参数完整展示给院长，经院长明确确认后才能执行**。遇到报错（参数错误、内容审核、连接失败等）**第一时间停止并向院长汇报**，禁止擅自换方案重试，禁止以"试一下"为由消耗 tokens。

## 立绘制作纪律（红线，强制）

> 本节是角色立绘出图的全局规范，**任何 AI 会话都必须遵守，不得因上下文截断而遗忘**。

1. **服饰一致性优先**：在没有明确剧情安排或用户明确指示的情况下，**同一角色的服饰、装扮、外表必须保持一致**。不能因为"重新出图"或"换表情"就改变服装、发色、配饰。服装变更须有剧情理由且经用户确认。

2. **表情差分原则（定版驱动）**：角色立绘先出**一张定版基准图**（base）。表情/动作差分（smile/cold/serious 等）基于这张定版图，通过 Seedream API（脸模+服装双参考）生成，**保证同一套服饰下的表情变化**，不能各出各的导致服装不统一。

3. **服饰须经用户过目**：角色的服饰装扮在正式批量出图前，**必须先出一张给用户过目确认**。用户确认后该服饰即锁定为该角色定版，后续表情差分均基于此。未确认不得批量出图。

4. **带背景原图必须留存**：AI 出图后，**带背景的原图必须保存**到 `.ai/seedream-test/` 或对应角色目录（已在 .gitignore）。抠图后的透明 PNG 入库 `public/`。**严禁只保存抠后结果**——否则后续换抠图方案时无法重抠。

5. **规划先于出图**：删除或重做立绘后，必须先输出「立绘规划清单」（谁出场、需要几套、每套服饰依据、表情需求），经用户确认后再出图。

6. **立绘展示位置统一**：所有立绘在游戏内展示时，**小腿中部位置对齐屏幕最下方水平线**。即立绘整体下移，脚部和小腿下半部分超出屏幕底部（不可见），上半身更突出。CSS 通过 `.char-portrait { bottom: 负值 }` 实现，具体偏移量需根据实际立绘比例调试确定。

7. **幸的立绘基准源自背景图**：幸的定版立绘以背景图 `tower_outdoor_mist` 中幸的形象为基准（锁服装/气质/姿态），先从背景图提取形象生成定版图，再做表情差分。院长和添的立绘按各自形象设计文档出图。

## 美术资产 README 纪律（红线，强制）

> **任何美术图片入库 `public/` 时，必须同步在美术资产 README 中登记并写明该图的内容。** AI 视觉模型对图片内容的描述不可靠（AGENTS 已多次记录视觉模型"描述雷同/模板化缓存/不可全信"），因此入库资产的内容必须由人工/AI 在出图时**以文字记录**，而非事后让模型看图反推。

1. **入库即登记**：每张图片（立绘/背景图/道具图/CG/表情差分）入 `public/` 的同一次提交，必须同步更新对应的 README。**入库图片但未补 README = 任务未完成**（等同代码改了文档没更新）。
2. **登记位置**：统一登记到 `prd/01-需求文档/04-德塔/02-设计/形象设计/美术资产/README.md`（现有清单表）。按资产类型分表：
   - 背景图 →「背景图」表（文件名 + 场景内容：地点/光线/人物入画情况/用途章节）
   - 立绘/表情差分 →「角色立绘 + 表情差分」表（角色 + 基准/差分 + 情绪 + 运行时目录）
   - 道具图 → 新增「道具图」表（文件名 + 道具内容：材质/纹样/魔法元素/触发节点）
   - CG → 归入背景图表并标注「CG-N」
3. **描述须写实际内容，不是文件名翻译**：必须写明图里画了什么——场景包含哪些人物、人物姿态/朝向、关键道具/标志、光线氛围、构图焦点。例如「谈判全景·对峙：四角色入画，幸左↔见右四目相对，添坐见右手一个身位，班楼梯口靠墙闭目，桌上羊皮卷隐隐发光」，而不是笼统的「谈判场景」。
4. **出图方负责写**：谁出图谁登记。黑机出图时同步补；白机若在代码层接入新资产，发现 README 缺该项，必须补登记后再算完成。

## AI 行为准则

1. **方案决策**：遇优先级判断或复杂决策，提供至少 2 种路径，对比业务价值与交付成本
2. **MECE 边界审查**：梳理需求时主动补全异常分支（断网、无权限、空状态、极值）
3. **检查点纪律**：完成重要输出后，总结已确认决策点、待验证假设、剩余待办
4. **暴露冲突**：规则相互矛盾时明确择一并阐明理由，禁止强行融合
5. **先查文档再动手**：执行任何不熟悉的操作前（生图、部署、配置变更等），**必须先读相关文档/handoff/技能 SKILL.md 恢复上下文**，确认流程和参数后再行动。上下文遗忘时主动回查，不懂就问，**禁止凭印象擅自行动**。

## 关键踩坑记录（必读）

完整记录见 `prd/01-需求文档/04-德塔/changelog.md` 和 `.ai/handoff.md` 的「德塔踩坑记录」表。高频坑：

- Colyseus 锁定 0.16.0（0.15 不兼容 schema 3.x，0.17 下载超时）
- Nginx `proxy_pass http://127.0.0.1:2567/;` 尾部斜杠必须（剥离 /ws 前缀）
- JWT 密钥用 `function getSecret()` 运行时读取（ESM import 提升会导致回退值）
- Phaser 场景切换用 `onUnmounted` → `destroyGame()`，不要用 `visibilitychange`
